{
  "name": "mortgage-simulator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "住宅ローン差額投資シミュレーター（単一 HTML）",
  "scripts": {
    "pretest": "node .claude/skills/deep-iterate/scripts/extract-inline-js.mjs index.html --out tests/generated",
    "test": "node --test tests/*.test.mjs"
  }
}
